import { Routes } from '@angular/router';
import { LayoutComponent } from './layout.component';

export const layouteRoutes: Routes = [
  {
    path: 'layout',
    component: LayoutComponent,
  },
];
